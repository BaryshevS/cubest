# Marker file so setuptools can ship the YAML profiles as package_data.
# The actual profiles are loaded by cubest.py via get_profiles_dir(),
# which reads YAML files directly — no imports from this package.
